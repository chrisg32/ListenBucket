//AI: Do not do any tasks tagged @human
Tasks:
  ✔ Fix issue with accessing mp3s from other devices @bug @ai @critical @done
    RSS feed now dynamically uses request host for audio URLs
    Frontend uses relative URLs (/api/media/{id}.mp3)
  ✔ Fix long descriptions make episode/source cards wider than the screen on mobile @bug @ai @done
    Added overflow-hidden, min-w-0, and break-words to card components
  ✔ Make the overcast button orange to match the overcast color (but still match the ListenBucket pallet) @ai @done
    Changed from blue to orange-100/orange-600/orange-900 colors
  ✔ Change the audioplayer to use amplitude.js and theme it to watch the site @ai @done
    Created custom AudioPlayer.vue component with themed UI (primary colors, dark mode support)
    Uses native HTML5 audio element with custom controls for better multi-instance support
  ✔ Make any changes nessesary to support audio scrubbing/seeking @ai @done
    AudioPlayer supports click-to-seek and drag-to-seek (mouse + touch)
    Progress bar shows current position with draggable handle
  ✔ Change RSS button to be gray @ai @done
    Changed from orange to gray-200/gray-700 colors
  ✔ Change RSS button to yellow @ai @done
    Changed to yellow-100/yellow-700/yellow-900 colors
  ✔ Create a publish_instructions.md file with instructions on how to deploy to DockerHub (and/or GitHub if it supports publishing container images) @ai @done
    Created publish_instructions.md with Docker Hub and GHCR instructions
  ✔ Create unit tests for anythig testable @ai @done
    Created internal/database/db_test.go with comprehensive tests
  ✔ Create a GitHub action CI/CD that will build and test that application @ai @done
    Created .github/workflows/ci.yml - runs tests, lint, and Docker build
  ✔ Make changes to README.md to be more typical of an open source project of this type @ai @done
    Restructured with badges, concise sections, collapsible API reference
  ✔ Copy any instructions about the project design into a new file for AI agents to reference (moved from README) @ai @done
    Created ARCHITECTURE.md with full technical documentation
  ✔ Create a GitHub action for building and publishing to GitHub @ai @done
    Created .github/workflows/release.yml - builds multi-arch images, pushes to Docker Hub and GHCR
  ✔ Update the README with relevant badges (build, test coverage, etc.) @ai @done
    Added CI, Release, Go Report Card, and License badges
  ✔ Add ListenBucket logo to the repo @human @done (26-02-02 17:00)
    Added listen-later.png
  ✔ After logo is available, replace the ListenLater feed image with the ListenBucket logo rather than from sources @ai @done
    RSS feeds now use /api/logo.png when feed has no image
    Logo embedded in Go binary via internal/assets
  ✔ After the logo is available, set the favicon to the logo @ai @done
    Updated web/index.html to use /logo.png as favicon
    Copied logo to web/public/logo.png
  ✔ Fix the ListenLater feed image is not the ListenBucket logo or the webpage cannot locate the image @ai @bug @done
    Updated FeedCard.vue and FeedView.vue to use /logo.png as fallback when image_url is empty
  ✔ The image in the README is too big. Please constrain the size of the image. @ai @done
    Used HTML img tag with width="200" and centered alignment
  ✔ Change the license from MIT to Apache-2.0. @ai @done
    Updated LICENSE file to Apache 2.0, updated README badge and license section
    ☐ Directly upload a file or a video (file upload)
    ✘ Multiple users @cancelled (26-02-02 21:54)
    ☐ Authentication/Authorization (for everything except feed downloads)
      ☐ Need an initial onboarding screen creating the initial account
    ✘ Cloud mirroring - will upload feed contents (xml + mp3s) to a cloud provider and use the external cloud provider link for opening in podcast apps, easy way to get around firewalls @cancelled (26-02-02 21:52)
      ✘ S3 bucket mirror @cancelled (26-02-02 21:52)
